#include "Player.h"
#include "Board.h"

void Player::getMove(int* coords, Board* board){
    //aak player to enter 3 integers
    //check that none are larger than the size of the board and the spot is not taken
    //if either is true, inform the player
}
int Player::getValue(){
    return val;
}

char Player::getToken(){
    return token;
}
